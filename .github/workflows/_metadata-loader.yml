name: Metadata Loader (reusable)
on:
  workflow_call:
    inputs:
      loader:
        required: true
        type: string

jobs:
  metadata:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Generate metadata
        run: |
          echo "Generating metadata for ${{ inputs.loader }}"
